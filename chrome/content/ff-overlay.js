mailru_session_manager.onFirefoxLoad = function(event) {
};

mailru_session_manager.showFirefoxContextMenu = function(event) {
};

window.addEventListener("load", function () { mailru_session_manager.onFirefoxLoad(); }, false);

